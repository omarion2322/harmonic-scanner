# Core data and market data libraries
defeatbeta-api>=0.0.34  # Fast, reliable market data from Hugging Face (no rate limits!)
yfinance>=0.2.31  # Kept as fallback (but defeatbeta-api is now primary)
pandas>=2.0.0
numpy>=2.2.5  # Required by defeatbeta-api (Python 3.10+ required)
pyarrow>=14.0.0
pyharmonics

# Scientific computing
scipy>=1.10.0

# Plotting and visualization
matplotlib>=3.7.0
mplfinance>=0.12.10b0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# Type checking and linting (optional but recommended)
mypy>=1.5.0
ruff>=0.1.0

# Data validation and settings management
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async HTTP client
aiohttp>=3.9.0

# Optional: Uncomment if you want technical indicators (requires system dependencies)
# ta-lib>=0.4.28

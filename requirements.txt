# Core data and market data libraries
defeatbeta-api @ git+https://github.com/defeat-beta/defeatbeta-api.git@92111b8c48b797d413cc96f13432c006cb592f69  # 0.0.61 fixes the data/US dataset paths; not yet on PyPI
yfinance>=0.2.31  # Kept as fallback (but defeatbeta-api is now primary)
pandas>=2.0.0
numpy>=2.2.5  # Required by defeatbeta-api (Python 3.10+ required)
pyarrow>=14.0.0
pyharmonics

# Scientific computing
scipy>=1.10.0

# Plotting and visualization
matplotlib>=3.7.0
mplfinance>=0.12.10b0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# Type checking and linting (optional but recommended)
mypy>=1.5.0
ruff>=0.1.0

# Data validation and settings management
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async HTTP client
aiohttp>=3.9.0

# Optional: Uncomment if you want technical indicators (requires system dependencies)
# ta-lib>=0.4.28
